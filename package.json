{
  "name": "@uzeen/random-number",
  "version": "1.0.1",
  "main": "dist/index.js",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/@Uzeen"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "author": "Uzeen Chhabra",
  "license": "ISC",
  "dependencies": {
    "@nestjs/common": "^10.4.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
